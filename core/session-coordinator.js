/* Hard account boundary: stale account state/listeners cannot leak into the next session. */
(function(){"use strict";let activeUid=null,generation=0;function clear(){['currentChatUid','currentChatName','callSessionId'].forEach(k=>localStorage.removeItem(k));try{sessionStorage.removeItem('she_active_chat')}catch(_){};window.SheAccountGeneration=generation}function attach(){const s=window.SheAuthSession;if(!s?.subscribe)return false;s.subscribe(user=>{const uid=user?.uid||null;if(uid!==activeUid){const previous=activeUid;generation++;if(previous&&previous!==uid)clear();activeUid=uid;window.SheAccountGeneration=generation;window.dispatchEvent(new CustomEvent('she:account-changed',{detail:{uid,previous,generation}}))}});return true}if(!attach())window.addEventListener('she:firebase-ready',attach,{once:true});})();
