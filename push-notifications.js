(()=>{
const VAPID_PUBLIC_KEY='BOO7jaXg1FQ4fgnb08wiIdeA8x4s01z9Ufq-b1c4IBpCoAv5obcv69rSlxeVh1gzJV0axaBeZJjtT8xzhb0QcwY';
const SW_URL='/firebase-messaging-sw.js?v=9';
let pushReady=false,hooked=false,booting=false;
async function firebase(){const A=await import('https://www.gstatic.com/firebasejs/10.12.2/firebase-app.js');const U=await import('https://www.gstatic.com/firebasejs/10.12.2/firebase-auth.js');const F=await import('https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore.js');const{firebaseConfig}=await import('./firebase-config.js');const app=A.getApps().length?A.getApps()[0]:A.initializeApp(firebaseConfig);return{auth:U.getAuth(app),db:F.getFirestore(app),F,U}}
function key(b){const p='='.repeat((4-b.length%4)%4),s=(b+p).replace(/-/g,'+').replace(/_/g,'/'),r=atob(s);return Uint8Array.from(r,c=>c.charCodeAt(0))}
// Background notification registration is intentionally silent. Do not create an in-page sync/status toast.
function status(){}
async function waitUser(auth){if(auth.currentUser)return auth.currentUser;return new Promise((resolve,reject)=>{let done=false;const timer=setTimeout(()=>{if(!done){done=true;unsub();reject(Error('AUTH_TIMEOUT'))}},30000);const unsub=auth.onAuthStateChanged(u=>{if(u&&!done){done=true;clearTimeout(timer);unsub();resolve(u)}})})}
async function register(){if(!('Notification'in window)||!('serviceWorker'in navigator)||!('PushManager'in window)||!window.isSecureContext)throw Error('UNSUPPORTED');const{auth,db,F}=await firebase();const u=await waitUser(auth);const reg=await navigator.serviceWorker.register(SW_URL,{scope:'/',updateViaCache:'none'});try{await reg.update()}catch(e){console.warn('SERVICE WORKER UPDATE',e)}await navigator.serviceWorker.ready;let permission=Notification.permission;if(permission==='default')permission=await Notification.requestPermission();if(permission!=='granted')throw Error(permission==='denied'?'BLOCKED':'PERMISSION');let sub=await reg.pushManager.getSubscription();if(!sub)sub=await reg.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:key(VAPID_PUBLIC_KEY)});const json=sub.toJSON();if(!json?.endpoint||!json?.keys?.p256dh||!json?.keys?.auth)throw Error('INVALID_SUBSCRIPTION');await F.setDoc(F.doc(db,'pushSubscriptions',u.uid),{uid:u.uid,subscription:json,endpoint:json.endpoint,updatedAt:F.serverTimestamp()},{merge:true});pushReady=true;console.log('PUSH READY',u.uid,json.endpoint);return json}
async function get(uid){const{auth,db,F}=await firebase();if(!auth.currentUser||!uid)return null;const s=await F.getDoc(F.doc(db,'pushSubscriptions',uid));return s.exists()?s.data().subscription:null}
async function send(subscription,data){if(!subscription?.endpoint)return false;try{const r=await fetch('/api/push',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({subscription,data})});const raw=await r.text();if(!r.ok){console.error('PUSH DELIVERY FAILED',r.status,raw);return false}console.log('PUSH DELIVERY SUCCESS',data?.type||'new_message');return true}catch(e){console.error('PUSH API ERROR',e?.stack||e);return false}}
async function notifyMessage(uid,name,text,cid){if(!uid)return false;try{const sub=await get(uid);if(!sub){console.warn('NO PUSH SUBSCRIPTION FOR RECEIVER',uid);return false}return await send(sub,{type:'new_message',senderName:name||'New message',body:String(text||'New message').slice(0,160),conversationId:cid||''})}catch(e){console.error('MESSAGE PUSH ERROR',e?.stack||e);return false}}
function readComposer(){const input=document.getElementById('messageInput');if(!input)return'';return String(input.value??input.innerText??'').replace(/\u00a0/g,' ').trim()}
function hookSendMessage(){if(typeof window.sendMessage!=='function')return false;if(window.sendMessage.__shePushHook)return true;const original=window.sendMessage;const wrapped=async function(...args){const text=readComposer();const receiverUid=localStorage.getItem('currentChatUid');const result=await original.apply(this,args);if(text&&receiverUid){try{const{auth}=await firebase();const me=await waitUser(auth);if(receiverUid!==me.uid)await notifyMessage(receiverUid,me.displayName||me.email||'New message',text,receiverUid)}catch(e){console.error('OUTGOING MESSAGE PUSH ERROR',e?.stack||e)}}return result};wrapped.__shePushHook=true;window.sendMessage=wrapped;hooked=true;console.log('OUTGOING MESSAGE PUSH HOOKED');return true}
async function boot(){if(booting||pushReady)return;booting=true;try{await register();hookSendMessage()}catch(e){console.error('PUSH SETUP',e?.stack||e)}finally{booting=false}}
window.shePush={register,get,send,notifyMessage};window.enableCallNotifications=async()=>{try{await register();hookSendMessage()}catch(e){console.error('PUSH REGISTER ERROR',e?.stack||e)}};window.addEventListener('load',()=>setTimeout(boot,700));setInterval(()=>{if(!pushReady)boot();if(!hooked)hookSendMessage()},1500);
})();
