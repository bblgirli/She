(()=>{const CLOUD='nputpy2e',PRESET='she_images';let input=null;function makeInput(){if(input)return input;input=document.createElement('input');input.type='file';input.accept='image/*';input.style.display='none';document.body.appendChild(input);input.onchange=()=>{const f=input.files?.[0];if(f)previewAttachment(f);input.value=''};return input}function previewAttachment(file){const url=URL.createObjectURL(file),old=document.getElementById('attachmentImagePreview');if(old)old.remove();const box=document.createElement('div');box.id='attachmentImagePreview';box.innerHTML='<div class="attachment-preview-sheet"><img alt="Image preview"><div class="attachment-preview-actions"><button type="button" class="attachment-cancel">Cancel</button><button type="button" class="attachment-send">Send</button></div><div class="attachment-upload-error" style="color:#ff6b6b;text-align:center;font-size:13px;margin-top:8px;word-break:break-word"></div></div>';const st=document.createElement('style');st.textContent='#attachmentImagePreview{position:fixed;inset:0;z-index:100000;background:#000;display:flex;align-items:center;justify-content:center}.attachment-preview-sheet{width:100%;height:100%;display:flex;flex-direction:column;align-items:center;justify-content:center;padding:18px;box-sizing:border-box}.attachment-preview-sheet img{max-width:100%;max-height:calc(100vh - 120px);width:auto;height:auto;object-fit:contain;border-radius:10px}.attachment-preview-actions{width:100%;display:flex;gap:16px;margin-top:18px}.attachment-preview-actions button{flex:1;border:0;border-radius:24px;padding:12px;font-size:16px;font-weight:600}.attachment-cancel{background:#333;color:#fff}.attachment-send{background:#078b59;color:#fff}';document.head.appendChild(st);box.querySelector('img').src=url;document.body.appendChild(box);box.querySelector('.attachment-cancel').onclick=()=>{URL.revokeObjectURL(url);box.remove()};box.querySelector('.attachment-send').onclick=async()=>{const b=box.querySelector('.attachment-send'),err=box.querySelector('.attachment-upload-error');b.disabled=true;b.textContent='Uploading…';err.textContent='';try{const result=await sendImage(file);console.log('IMAGE SENT TO CLOUDINARY',result);URL.revokeObjectURL(url);box.remove()}catch(e){console.error('IMAGE SEND ERROR',e);b.disabled=false;b.textContent='Retry upload';err.textContent=e.message||'Upload failed'}}}function attachFile(){const i=makeInput();i.removeAttribute('capture');i.click()}function openCamera(){const i=makeInput();i.setAttribute('capture','environment');i.click()}window.attachFile=attachFile;window.openCamera=openCamera;async function uploadCloudinary(file){if(!file||!file.size)throw Error('IMAGE_EMPTY');const fd=new FormData();fd.append('file',file,file.name||'image');fd.append('upload_preset',PRESET);const endpoint=`https://api.cloudinary.com/v1_1/${CLOUD}/image/upload`;const r=await fetch(endpoint,{method:'POST',body:fd});const body=await r.text();let j;try{j=JSON.parse(body)}catch(_){j={error:{message:body}}}if(!r.ok||!j.secure_url){console.error('Cloudinary upload failed',r.status,j);throw Error('Cloudinary: '+(j.error?.message||`HTTP ${r.status}`))}console.log('Cloudinary upload success',j.public_id,j.width,j.height,j.bytes,j.secure_url);return j}async function sendImage(file){const uploaded=await uploadCloudinary(file);return uploaded}window.sendImageFile=sendImage;window.sendImage=sendImage;makeInput()})();