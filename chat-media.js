(()=>{let input=null;function makeInput(){if(input)return input;input=document.createElement('input');input.type='file';input.accept='image/*';input.capture='environment';input.style.display='none';document.body.appendChild(input);input.onchange=()=>{const f=input.files?.[0];if(f)sendImage(f);input.value=''};return input}async function openCamera(){makeInput().click()}async function attachFile(){const i=makeInput();i.removeAttribute('capture');i.click()}window.openCamera=openCamera;window.attachFile=attachFile;async function fb(){const {firebaseConfig}=await import('./firebase-config.js');const A=await import('https://www.gstatic.com/firebasejs/10.12.2/firebase-app.js');const U=await import('https://www.gstatic.com/firebasejs/10.12.2/firebase-auth.js');const F=await import('https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore.js');const app=A.getApps().length?A.getApps()[0]:A.initializeApp(firebaseConfig);return{auth:U.getAuth(app),db:F.getFirestore(app),...F}}async function sendImage(file){try{const{auth,db,collection,addDoc,doc,setDoc,serverTimestamp}=await fb();const other=localStorage.getItem('currentChatUid');if(!auth.currentUser||!other)throw Error('Chat session unavailable');const cid=[auth.currentUser.uid,other].sort().join('_');const data=await new Promise((ok,no)=>{const r=new FileReader();r.onload=()=>ok(r.result);r.onerror=()=>no(r.error);r.readAsDataURL(file)});let imageData=data;if(typeof data==='string'&&data.length>900000)imageData=await downscale(data,2048,.92);await addDoc(collection(db,'conversations',cid,'messages'),{senderId:auth.currentUser.uid,receiverId:other,imageData,text:'[Image]',createdAt:serverTimestamp(),status:'sent',imageName:file.name,imageType:file.type||'image/jpeg'});await setDoc(doc(db,'conversations',cid),{participants:[auth.currentUser.uid,other],lastMessage:'[Image]',lastMessageSenderId:auth.currentUser.uid,lastMessageTime:serverTimestamp(),updatedAt:serverTimestamp(),unreadBy:[other]},{merge:true})}catch(e){console.error(e);window.showError?.('Failed to send image')}}function downscale(data,max,q){return new Promise(ok=>{const i=new Image();i.onload=()=>{const s=Math.min(1,max/Math.max(i.naturalWidth,i.naturalHeight));const c=document.createElement('canvas');c.width=Math.round(i.naturalWidth*s);c.height=Math.round(i.naturalHeight*s);c.getContext('2d').drawImage(i,0,0,c.width,c.height);ok(c.toDataURL('image/jpeg',q))};i.onerror=()=>ok(data);i.src=data})}makeInput()})();